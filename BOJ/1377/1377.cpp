#include<cstdio>
#include<vector>
#include<algorithm>

using namespace std;

int n, a[500001], b[500001];


int main(){
	scanf("%d", &n);
	for(int i = 0; i< n; i++) scanf("%d", &a[i]), b[i]=a[i];
	sort(a, a+n);
	for(int i = 0; i < n; i++){
		int it = binary_
	}
	process();
}
